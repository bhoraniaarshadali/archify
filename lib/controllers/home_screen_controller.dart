import 'package:get/get.dart';

class HomeScreenController extends GetxController {
  // Add any home-specific logic here if needed.
  // The user just requested that it have an update(["full"]) capability in Get.
}
